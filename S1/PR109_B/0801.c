#include <stdio.h>

int funtion(int a)
{
        printf("%d", ,)
    return ;
}
int main(void)
{
    int n, *m, *k;
    while (scanf("%d", &n) != EOF && n >= 0) {
        if (n != 0) {
            *m = n;
        }
        else
            k++;
        printf("%d\n", funtion(n));
    }
    return 0;
}
